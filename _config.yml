title: "My Site"
theme: minima      # Use a supported default theme
plugins: []

# Optional: default page layout
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
